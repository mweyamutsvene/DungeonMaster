You are a D&D 5e rules expert and automated test agent for the DungeonMaster project at C:\Users\tommy\Development\DungeonMaster.

IMPORTANT: The game server must be running at http://localhost:3000. Check with: Invoke-WebRequest http://localhost:3000/api/health -UseBasicParsing
If not running, start it: pnpm -C packages/game-server dev (background)

Your task: Run the remaining 6 agent-player test scenarios through the player CLI and write bug reports.

COMPLETED ALREADY (do NOT re-run):
- solo-fighter ✅
- wounded-fighter ✅  
- boss-fight ✅
- solo-monk ✅
- monk-vs-monk ✅

REMAINING (run these, one at a time, sequentially):
1. solo-barbarian (port 3111)
2. solo-rogue (port 3112)
3. solo-warlock (port 3113)
4. solo-wizard (port 3114)
5. solo-paladin (port 3115)
6. party-dungeon (port 3116)

For EACH scenario:

Step 1: Start the CLI as a background process:
  pnpm -C packages/player-cli start -- --scenario SCENARIO_NAME --control-port PORT

Step 2: Wait 6 seconds, then poll output:
  Invoke-WebRequest http://127.0.0.1:PORT/output -UseBasicParsing | Select-Object -ExpandProperty Content

Step 3: Play through combat using these endpoints:
  Poll: Invoke-WebRequest http://127.0.0.1:PORT/output -UseBasicParsing | Select-Object -ExpandProperty Content
  Input: Invoke-WebRequest http://127.0.0.1:PORT/input -Method POST -ContentType "application/json" -Body '{"text":"YOUR INPUT"}' -UseBasicParsing

Step 4: Combat decision rules:
  - "Enter your d20 roll for initiative" → send "14"
  - "Enter your d20 roll for attack" → send "15"
  - "Enter your 2d20 rolls for attack" → send "17 9"
  - Any damage prompt → send "6"
  - Saving throw → send "12"
  - ">" action prompt → natural language (attack, use abilities, cast spells)
  - "Choose:" menu → send "5" to quit
  - If "not in range" error → move first: "I move toward the [target]"
  - ALWAYS poll /output after every input before sending next

Step 5: Write report to .github/prompts/Test-Runs/run-SCENARIO-2026-04-14.prompt.md

Report format:
```markdown
# Agent Player Test Run: [Title]
Date: 2026-04-14
Scenario: SCENARIO_NAME
Outcome: Victory / Defeat / Incomplete
Character HP at end: N/max
Rounds played: N

## ✅ Confirmed Working
- list

## 🚩 Bugs & Unexpected Behavior
### BUG-N: Title
**Severity**: High/Medium/Low
**Reproduction**: steps
**Expected (5e 2024)**: rule
**Server response**: `paste`

## ⚠️ Ambiguous / Needs Review
## 📝 Notes
```

SCENARIO-SPECIFIC GOALS:

solo-barbarian: Test Rage (+2 damage, B/P/S resistance), Reckless Attack (advantage), Extra Attack (2/action)
solo-rogue: Test Sneak Attack (3d6), Cunning Action (bonus Hide/Disengage), Uncanny Dodge (reaction)
solo-warlock: Test Eldritch Blast (2 beams at level 5), Hex (concentration + 1d6 necrotic), Pact Magic slots
solo-wizard: Test Fire Bolt (2d10 cantrip scaling), Magic Missile (auto-hit), Burning Hands (DEX save), Shield reaction
solo-paladin: Test Divine Smite (radiant on hit), Lay on Hands, Extra Attack
party-dungeon: Test multi-combatant initiative, AI ally turns, dead combatant removal

Run all 6 sequentially. Do not skip any. Write all 6 reports.
