{
  "name": "@dungeonmaster/rules-query-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP stdio server exposing read-only queries over the deterministic D&D rules engine. Consumed by both Claude Code and GitHub Copilot via mcp.json.",
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
